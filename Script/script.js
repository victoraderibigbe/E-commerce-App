$(document).ready(function() {
    // Activate carousel
    $("#carouselExampleIndicators").carousel();
  
    // Enable carousel auto sliding
    $(".carousel").carousel({
      interval: 1000 // Change interval time here
    });
});